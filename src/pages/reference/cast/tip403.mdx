---
description: "TIP-403 policy registry operations (Tempo)"
---

_Generated from `cast tip403 --help`; see [CLI reference versions](/reference/versions). Regenerate this page instead of editing it directly._

## cast tip403

TIP-403 policy registry operations (Tempo)

:::terminal

```bash
$ cast tip403 --help
```

```txt
Usage: cast tip403 [OPTIONS] <COMMAND>

Commands:
  create     Create a new simple (whitelist or blacklist) policy
  whitelist  Add or remove an account from a whitelist policy
  blacklist  Add or remove an account from a blacklist policy
  info       Show a policy's type and admin
  check      Check whether an address is authorized by a policy
  help       Print this message or the help of the given subcommand(s)

Options:
  -h, --help
          Print help (see a summary with '-h')

  -j, --threads <THREADS>
          Number of threads to use. Specifying 0 defaults to the number of
          logical cores
          
          [aliases: --jobs]

Display options:
      --color <COLOR>
          The color of the log messages

          Possible values:
          - auto:   Intelligently guess whether to use color output (default)
          - always: Force color output
          - never:  Force disable color output

      --json
          Format log messages as JSON

      --md
          Format log messages as Markdown

  -q, --quiet
          Do not print log messages

  -v, --verbosity...
          Verbosity level of the log messages.
          
          Pass multiple times to increase the verbosity (e.g. -v, -vv, -vvv).
          
          Depending on the context the verbosity levels have different meanings.
          
          For example, the verbosity levels of the EVM are:
          - 2 (-vv): Print logs for all tests.
          - 3 (-vvv): Print execution traces for failing tests.
          - 4 (-vvvv): Print execution traces for all tests, and setup traces
          for failing tests.
          - 5 (-vvvvv): Print execution and setup traces for all tests,
          including storage changes and
            backtraces with line numbers.
```

:::