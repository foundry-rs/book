# probe concat-hex

Concatenate hex strings

```bash
$ probe concat-hex --help
Usage: probe concat-hex [DATA]...

Arguments:
  [DATA]...  The data to concatenate

Options:
  -h, --help  Print help
```