# probe decode-transaction

Decodes a raw signed EIP 2718 typed transaction

```bash
$ probe decode-transaction --help
Usage: probe decode-transaction [TX]

Arguments:
  [TX]  

Options:
  -h, --help  Print help
```