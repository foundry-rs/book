# cast from-rlp

Decodes RLP hex-encoded data

```bash
$ cast from-rlp --help
```

```txt
Usage: cast from-rlp [VALUE]

Arguments:
  [VALUE]  The RLP hex-encoded data

Options:
  -h, --help  Print help
```