# probe to-ascii

Convert hex data to an ASCII string

```bash
$ probe to-ascii --help
Usage: probe to-ascii [HEXDATA]

Arguments:
  [HEXDATA]  The hex data to convert

Options:
  -h, --help  Print help
```