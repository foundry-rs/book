# probe to-rlp

RLP encodes hex data, or an array of hex data

```bash
$ probe to-rlp --help
Usage: probe to-rlp [VALUE]

Arguments:
  [VALUE]  The value to convert

Options:
  -h, --help  Print help
```