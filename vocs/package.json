{
  "name": "vocs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vocs dev",
    "build": "bun run fetch-releases && bun run fetch-benchmarks && vocs build",
    "preview": "bun run build && vocs preview",
    "check-links": "bun check-links.ts",
    "fetch-releases": "bun scripts/fetch-releases.ts",
    "fetch-benchmarks": "bun scripts/fetch-benchmarks.ts"
  },
  "dependencies": {
    "react": "latest",
    "react-dom": "latest",
    "vocs": "latest"
  },
  "devDependencies": {
    "@types/bun": "^1.2.18",
    "@types/react": "latest",
    "typescript": "latest"
  }
}